extends layout

block content
  h1= title
  p Welcome to #{title}
  div
    table
      thead
        tr 
          th Crime ID
          th Crime Name
      tbody
        each course, i in courses
          tr(class=(i % 2 == 0) ? 'odd' : 'even')
           td= course.cid

